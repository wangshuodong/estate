<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wangsd.web.dao.DepartmentMapper">

    <!-- 根据部门code查询所有公司 -->
    <select id="queryDepartmentListByCode" parameterType="Department" resultType="DepartmentCustom">
        SELECT
            d.*, parent.`name` as parentName
        FROM
            department d
        LEFT JOIN department parent ON d.parent_id = parent.id
        WHERE
            d.deleteStatus = FALSE
        AND d.type = #{type}
        AND d. CODE LIKE CONCAT(#{code},'%')
    </select>

    <!-- 查询parent下面的最大code -->
    <select id="selectMaxByParentCode" parameterType="int" resultType="string">
        select max(code) from department d where d.parent_id = #{parentId}
    </select>
</mapper>