<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.wangsd.web.dao.RoominfoMapper">
    <!-- 查询所有房屋信息 -->
    <select id="queryRoominfoList" parameterType="RoominfoCustom" resultType="RoominfoCustom">
        SELECT
            r.*, d.`name` as deptName
        FROM
            roominfo r
        JOIN housinginfo d ON r.parent_id = d.id
        <where>
            and d.deleteStatus = FALSE
            <if test="departmentCode != null and departmentCode != ''">
                and d.CODE LIKE CONCAT(#{departmentCode},'%')
            </if>
            <if test="status != null">
                and r.status = #{status}
            </if>
            <if test="parentId != null and parentId != ''">
                and r.parent_id = #{parentId}
            </if>
        </where>

    </select>

    <!-- 查询distinct房屋信息 -->
    <select id="queryRoominfoDistinct" parameterType="RoominfoCustom" resultType="RoominfoCustom">
        SELECT
        r.group_name,r.building,r.unit,r.room
        FROM
        roominfo r
        JOIN department d ON r.department_id = d.id
        WHERE
        d.deleteStatus = FALSE
        and d.CODE LIKE CONCAT(#{departmentCode},'%') group by r.group_name,r.building,r.unit,r.room
    </select>
</mapper>